{
  "name": "@curtisbelt/github-label-setup",
  "private": true,
  "version": "0.0.0",
  "description": "Apply consistent Github labels across repositories.",
  "files": [
    "labels.json"
  ],
  "scripts": {
    "release": "standard-version",
    "reinstall": "rm -R ./node_modules; rm yarn.lock; yarn install",
    "generate-labels-markdown": "node generate-labels-markdown.js"
  },
  "repository": "https://github.com/CurtisBelt/github-label-setup",
  "devDependencies": {
    "github-label-sync": "^1.4.1",
    "standard-version": "^6.0.1"
  }
}
